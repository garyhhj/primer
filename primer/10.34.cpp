#include <iostream>
#include <iterator>
#include <vector>

/*
int main() {

	std::vector<int> vi = { 1, 2, 3, 4, 5 }; 

	std::ostream_iterator<int> os_it(std::cout, " "); 

	//one line solution 
	//std::copy(vi.rbegin(), vi.rend(), os_it); 

	for (auto it = vi.rbegin(); it != vi.rend(); ++it) {
		std::cout << *it << " "; 
	}
}
*/