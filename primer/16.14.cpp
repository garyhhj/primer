#include <iostream>

/*
template <typename T> 
class Screen {
public: 
	Screen<T>() = default; 
	
	void get() {
		std::cout << "height " << Screen<T>::height << std::endl; 
		std::cout << "width " << Screen<T>::width << std::endl; 
	}

	void set(const T& newHeight, const T& newWidth) {
		Screen<T>::height = newHeight; 
		Screen<T>::width = newWidth; 
	}

	T height; 
	T width; 
};

int main() {
	Screen<int> s; 
	s.set(1, 2); 
	s.get(); 
	
}
*/