#include <iostream>
#include <string>
/*
class Person {
public:
	Person() = default;
	Person(const std::string& initialName, const std::string& initialAddress) : name(initialName), address(initialAddress) {}

	//functions should be const since we aren't changing the name or address
	std::string getName() const { return name; }
	std::string getAddress() const { return address; }

private:
	std::string name;
	std::string address;
};

int main() {
	Person p1; 
	std::cout << p1.getName() << " " << p1.getAddress() << std::endl; 

	Person p2("cat", "catland"); 
	std::cout << p2.getName() << " " << p2.getAddress() << std::endl;

}
*/
