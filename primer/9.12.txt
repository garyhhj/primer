the one that takes a container to copy will require the types to match exactly 

the one that takes two iterator will copy the values between those ranges and allow different types as long as the types can convert 