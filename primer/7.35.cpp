#include <string>
/*
using namespace std; 

typedef char Type;
Type initVal();
class Exercise {
public:
	typedef double Type;
	Type setVal(Type); //double setVal(double)
	Type initVal(); //double initVal()
private:
	int val;
};

//Type Exercise::setVal(Type parm) { // char Exercise::setVal(double parm)
double Exercise::setVal(double parm){
	val = parm + initVal(); return val;
}
*/

