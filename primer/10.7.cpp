#include <iostream>
#include <vector>
#include <numeric>
#include <list>

/*
int main() {
	
	//part a 
	std::vector<int> vec; 
	std::list<int> lst; 
	int i; 
	while (std::cin >> i) 
		lst.push_back(i); 
	//copy(lst.cbegin(), lst.cend(), vec.cbegin())
	copy(lst.cbegin(), lst.cend(), std::back_inserter(vec)); 

	for (auto i : vec) {
		std::cout << i << ' ';
	}
	std::cout << std::endl;
	
	//clearing vec and lst  
	vec.clear(); 
	lst.clear(); 
	

	//part b
	//vec.reserve(10); 
	vec.resize(10); 
	std::fill_n(vec.begin(), 10, 0); 


	for (auto i : vec) {
		std::cout << i << ' '; 
	}
	std::cout << std::endl; 

}
*/

