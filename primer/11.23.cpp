#include <map>
#include <iostream>
#include <string>

/*
void print(std::multimap<std::string, std::string>& lname_fname) {
	
	std::cout << "printing map: \n";
	for (auto& i : lname_fname) {
		std::cout << i.first << " " << i.second << "\n"; 
	}

}

void read(std::multimap<std::string, std::string>& lname_fname) {
	
	std::string lname, fname; 
	while (std::cin >> lname >> fname) {
		lname_fname.insert({ lname, fname }); 
	}
}

int main() {
	std::multimap<std::string, std::string> lname_fname; 
	read(lname_fname); 
	print(lname_fname); 


}
*/