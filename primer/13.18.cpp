#include <iostream>
#include <string>

/*
class Employee {
public: 

	Employee() : name(std::string()), id(++uid) {}
	Employee(const std::string newName) : name(newName), id(++uid) {}

	std::string name; 
	int id; 

private: 
	static int initUid(); 
	static int uid; 
};

int Employee::initUid() { return 1; }
int Employee::uid = Employee::initUid(); 

int main() {
	Employee g; 
	std::cout << g.name << " " << g.id << std::endl; 
}
*/