#include <iostream>
#include <string>

/*
std::string concat(const std::string& s1, const std::string& s2) {
	std::string s12 = "";
	
	s12 += s1; 
	s12 += s2; 
	return s12; 
}

int main() {
	std::string s12 = concat("meow", "woof"); 

	char* c = new char[s12.size() + 1]; 
	
	for (int i = 0; i <= s12.size(); ++i) {
		c[i] = s12[i]; 
	}

	//std::cout << int(s12[s12.size()]); 
	std::cout << c; 

	delete[] c;
}
*/