#include <map>
#include <iostream>

/*
int main() {
	std::map<int, int> mp = { {1, 2} }; 

	std::map<int, int>::iterator mp_it = mp.begin(); 
	if (mp_it != mp.end()) {
		mp_it->second = 0; 
	}

	std::cout << mp_it->first << " : " << mp_it->second; 
}
*/
