#include <iostream>
#include <unordered_map>
#include <string>

/*
void print(std::unordered_map<std::string, int>& word_count) {
	for (auto& i : word_count) {
		std::cout << i.first << " : " << i.second << "\n"; 
	}
}

void count_words(std::unordered_map<std::string, int>& word_count) {
	std::string word; 
	while (std::cin >> word) {
		++word_count[word]; 
	}
}

int main() {
	std::unordered_map<std::string, int> word_count; 
	
	count_words(word_count); 
	print(word_count); 
}
*/