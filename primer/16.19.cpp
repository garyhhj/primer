#include <iostream>
#include <vector>

/*
template <typename T> 
void print(const T& container) {
	typedef typename T::size_type n; 

	std::cout << "conatiner: "; 
	for (n i = 0; i < container.size(); ++i) {
		std::cout << container[i] << ", "; 
	}
	std::cout << std::endl; 
}

int main() {

	std::vector<int> meow = { 1, 2, 3, 4 }; 
	print(meow); 
}*/