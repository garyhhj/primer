#include <vector>
#include <list>
#include <algorithm>
#include <iostream>
/*
int main() {
	std::vector<int> vi = { 1, 2, 3, 4, 5, 6, 7, 8, 9 };
	std::list<int> lifi;
	std::list<int> libi;
	std::list<int> lii;



	std::unique_copy(vi.begin(), vi.end(), std::back_inserter(libi));
	std::unique_copy(vi.begin(), vi.end(), std::front_inserter(lifi));
	std::unique_copy(vi.begin(), vi.end(), std::inserter(lii, lii.begin()));

	for (auto i : lifi) std::cout << i << " ";
	std::cout << "\n";


	for (auto i : libi) std::cout << i << " ";
	std::cout << "\n";


	for (auto i : lii) std::cout << i << " ";
	std::cout << "\n";
}
*/