#include <iostream>
#include <algorithm>
#include <vector>

/*
int main() {
	std::vector<int> vi;

	int ele; 
	while (std::cin >> ele) {
		vi.push_back(ele); 
	}

	int freq = std::count(vi.cbegin(), vi.cend(), 2); 
	std::cout << freq << std::endl; 
}
*/

