#include <vector>
#include <utility>
#include <iostream>
#include <string>

/*
int main() {
	std::vector<std::pair<std::string, int>> vsi; 

	std::string word; 
	int num; 
	while (std::cin>> word >> num) {

		vsi.push_back({ word, num }); 
		//vsi.push_back(std::make_pair(word, num)); 
		//vsi.push_back(std::pair<std::string, int>(word, num)); 
	}

	for (auto& si : vsi) {
		std::cout << si.first << " " << si.second << "\n"; 
	}

	std::cout << vsi.size() << std::endl;
}
*/
