#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

/*
bool lessThanFive(const std::string& s) {
	return s.size() >= 5; 
}

void print(const std::vector<std::string>& vs) {
	for (auto s : vs) {
		std::cout << s << ' ';
	}
	std::cout << std::endl; 
}

int main() {
	std::vector<std::string> vs; 
	std::string s; 

	while (std::cin >> s) {
		vs.push_back(s); 
	}

	std::partition(vs.begin(), vs.end(), lessThanFive); 
	print(vs); 

}
*/