#include <iostream>
#include <string>

/*
int main() {

	int len;
	std::cin >> len;
	char* cptr = new char[len + 1];
	
	std::cin.get(cptr, len + 1); 
	
	//if iostream has more characters than dynamically allocated, those characters will be ignored 

	std::cout << cptr << std::endl; 
	
}
*/