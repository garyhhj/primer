#include <vector>
#include <iostream>

/*
bool hasVal(std::vector<int>::const_iterator begin, std::vector<int>::const_iterator end, int val) {
	while (begin != end) {
		if (*begin == val) {
			return true; 
		}
		++begin; 
	}
	return false; 
}

int main() {
	std::vector<int> v; 
	for (int i = 0; i <= 10; ++i) {
		v.push_back(i); 
	}

	int val; 
	std::cin >> val; 

	std::cout << hasVal(v.begin(), v.end(), val) << std::endl; 

	return 0; 
}
*/