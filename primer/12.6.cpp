#include <iostream>
#include <vector>

/*
std::vector<int>* factory() {
	return new std::vector<int>; 
}

void read(std::vector<int>* pvi) {
	int i; 
	while (std::cin >> i) {
		pvi->push_back(i); 
	}
}

void print(std::vector<int>* pvi) {
	for (auto i : *pvi) {
		std::cout << i << "\n";
	}
	std::cout << std::flush;
}



int main() {
	//allocate memory 
	auto* pvi = factory(); 
	read(pvi); 

	std::cout << '\n'; 
	print(pvi); 

	//delete memory 
	delete pvi; 

	
}
*/